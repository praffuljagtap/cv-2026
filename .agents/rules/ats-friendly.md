---
trigger: always_on
---

- Structure with Semantic HTML: Use tags like <header>, <main>, <section>, and <article> to define the document hierarchy. Wrap each job entry in an <article> and use the <time> tag for dates to ensure accurate machine parsing.

- Prioritize Standard Web-Safe Fonts: Use high-legibility fonts like Arial, Calibri, or Times New Roman. Avoid custom or decorative web fonts that may fail to embed correctly in the final PDF export.

- Implement Keyword Mirroring: Ensure the resume content matches the specific terminology used in the target job description (e.g., use "Project Management" instead of "Leading teams").

- Use Strong Action Verbs: Begin every bullet point in the experience section with a high-impact action verb such as "Developed," "Optimized," or "Managed."

- Quantify Achievements: For every major responsibility, include at least one metric or numerical result (e.g., "reduced latency by 15%" or "managed a $50k budget").
